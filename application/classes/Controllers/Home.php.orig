<?
namespace controllers;

use application\models\database\Record;
use application\models\test\Data;
use application\models\test\Data2;
use spiral\classes\html\Parser;
use spiral\classes\Json;
use spiral\classes\rest\Query;
use spiral\core\CLI;
use spiral\core\Controller;
use spiral\core\http\FileStream;
use spiral\helpers\HttpHelper;
use spiralScout\pinterest\API;

class Home extends Controller
{
    /**
     * Basic action.
     *
     * @return string
     */
    public function actionIndex()
    {
        $a['a'] = 1;

        unset($a['x']['v']);

        return '';

        return $this->view->render('home', array('value' => mt_rand(0, 100)));
    }
}